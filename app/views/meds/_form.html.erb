<%= simple_form_for(@med) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :strength %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
